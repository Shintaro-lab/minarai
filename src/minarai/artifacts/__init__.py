"""Artifact loading, validation and rendering.

An Artifact YAML file is the Source of Truth for reviewable content
(design docs, specs, test cases, ...). HTML rendered from it is always a
disposable, regenerable view — never edited directly.
"""
